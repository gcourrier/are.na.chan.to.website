name: Example Workflow

on:
  push:
    branches:
      - main

jobs:
  example:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Use Secret in Script
        run: |
          echo "API Token is ${{ secrets.ARENA_API_TOKEN }}"
