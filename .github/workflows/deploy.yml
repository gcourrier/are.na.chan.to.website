name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and Deploy
        env:
          ARENA_API_TOKEN: ${{ secrets.ARENA_API_TOKEN }}
        run: |
          echo "Building site..."
          # Example: Create a file that includes the token
          echo "const arenaToken = '${ARENA_API_TOKEN}';" > env.js
          # Run your build or deployment commands here
